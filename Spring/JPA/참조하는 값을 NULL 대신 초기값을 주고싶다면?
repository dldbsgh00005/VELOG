# 참조하는 값이 있는데 생성 할 때 NULL 값일 가능성이 있는 경우도 있다.

이 것은 외래키의 특성(필수/옵셔널)에 따라 다르게 처리할 수 있다.

1. 필수 -> 필드를 생성자 형태 Type variableName = new Type(); 로 할당하고
  @Builder를 사용 중이라면 필드에도 @Builder.Default(초기화를 해주겠다는 어노테이션)를 설정해준다.

2. 옵셔널 값 -> 필수처럼 처리해줄 수도 있고, null 값으로 비워둘 수도 있다. ( 추후 추가시 적절한 값을 삽입한다.)
