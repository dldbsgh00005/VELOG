`@EventListener`을 `SpringBootApplication` 위에 작성 시 애플리케이션 시작 시에 전체 EventListener를 스캔한다.
EventListener를 `ApplicationListener` 인터페이스를 통해 직접 구현할 수도 있다.

`ApplicationEventPublisher` 클래스를 통해 이벤트를 발행한다.
AppicationContext
